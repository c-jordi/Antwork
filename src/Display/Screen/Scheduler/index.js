import React from "react";
import Interface from "./Interface/index";

const Scheduler = () => {
	return (
		<div className="page scheduler">
			<div className="title">Scheduler</div>
			<div className="body">
				Left click and drag to move them around Right click to create,
			</div>
		</div>
	);
};
export default Scheduler;
