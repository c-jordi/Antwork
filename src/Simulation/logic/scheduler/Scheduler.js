function Scheduler() {
	this.timeline = [
		{ id: "Walker_0", type: "walker", events: [{ frame: 200 }] }
	];
}

// Scheduler.prototype.convert =function(){
//     this.timeline
// }

/*
[
    {id:"",type:"",events:[
        {t:}
    ]}
]


*/

export default Scheduler;
